# Network Deployment Commands
## Ethereum
- `npm run deploy-token-creator -- --network mainnet`
- `npm run deploy-bulksender -- --network mainnet`

## Sepolia
- `npm run deploy-token-creator -- --network sepolia`
- `npm run deploy-bulksender -- --network sepolia`

## Avalance
- `npm run deploy-token-creator -- --network avalanche`
- `npm run deploy-bulksender -- --network avalanche`

## Uniswap
- `npm run deploy-token-creator -- --network uniswap`
- `npm run deploy-bulksender -- --network uniswap`

## Berachain
- `npm run deploy-token-creator -- --network berachain`
- `npm run deploy-bulksender -- --network berachain`

## Blast
- `npm run deploy-token-creator -- --network blast`
- `npm run deploy-bulksender -- --network blast`

## Cronos
- `npm run deploy-token-creator -- --network cronos`
- `npm run deploy-bulksender -- --network cronos`
